export interface iFilterControlProps {
  // setFilters: (filterName: string, filterValue: string | null) => void;
  // clearFilters: () => void;
  // filters: {
  //   motivo: string;
  // };
  setFilterMotivo: (motivo: string) => void;
}
